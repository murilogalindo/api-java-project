image: maven:3.1.2
stages:
  - test

cache:
  paths:
    - target

test_api:
  stage:
  - test
  - build
  - deploy
  image: maven:3.1.2-eclipse-temurin-20
  script:
    - mvn clean test
  artifacts:
    when: always
    paths:
      - target/gitlab-pipeline.jar
    expire_in: 1 week
  only:
    - main
